import { Product, createProduct } from './product';
import { Cart, createCart } from './cart';
import { extendArray } from './extendArray';
import { isValid } from './isValid';

function main() {
    // const product: Product = createProduct("Danh", 1000);
    // console.log(product);
    // product.input();
    // console.log(product);

    // const cart: Cart = createCart();
    // cart.input();
    // console.log(cart);
    // cart.priceTotal();

    // let arr1: number[] = [1, 2, 3];
    // let arr2: number[] = [4, 5, 6];
    // let x: number[] = extendArray(arr1, arr2);
    // console.log(x);

    // let y: boolean = isValid(product);
    // console.log(y);
}

main();